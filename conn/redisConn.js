// const redis = require('redis')
// const {promisify} = require("util")
//
// const cl = redis.createClient({host: 'redis'})
module.exports = {
    // getAsync: async (key) => {
    //     const bound = promisify(cl.get).bind(cl)
    //
    //     return await bound(key)
    // },
    // setAsync: async (sessId, value) => {
    //     const bound = promisify(cl.set).bind(cl)
    //
    //     return await bound(sessId, value)
    // }
}


